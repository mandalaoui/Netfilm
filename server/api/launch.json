{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch API Server",
            "program": "${workspaceFolder}/server/api/server.js",
            "envFile": "${workspaceFolder}/server/api/config/.env.local",
            "cwd": "${workspaceFolder}/server/api",
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}